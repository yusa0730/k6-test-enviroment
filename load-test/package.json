{
  "name": "load-test",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke:local": "dotenvx run -f .env -- k6 run scenarios/rest-smoke.js",
    "sst:deploy": "sst deploy --print-logs",
    "sst:diff": "sst diff --print-logs",
    "sst:remove": "sst remove --print-logs"
  },
  "dependencies": {
    "infra": "workspace:*"
  },
  "devDependencies": {
    "@dotenvx/dotenvx": "^1.39.0",
    "sst": "^4.17.0"
  }
}
